package com.project.moneytransfer.Enums;

// This enums defines customer's type
// Blocked (if user wish this. Data of user anyway will be stored in database. It won't be removed)
// Active (ordinary type)
public enum CustomerStatus {
    BLOCKED,
    ACTIVE
}
