package com.project.moneytransfer.Enums;

// Enum that defines account's type
// it can be bonus card
// or ordinary
// or main, where all money will come to
public enum AccountType {
    MAIN,
    ORDINARY
}
